p = float(input("what is the value of principal amount? "))
r = float(input("what is the value of interest rate? "))
t = float(input("what is the value of time duration? "))

interest = (p*r*t)/100
prinlabt(interest)